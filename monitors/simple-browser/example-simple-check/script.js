/**
 * Simple Browser Monitor
 * 
 * Note: Simple browser monitors don't use custom scripts.
 * Configuration is handled entirely in config.json.
 * This file exists as a placeholder for consistency.
 * 
 * The monitor will:
 * - Visit the URL specified in config.json (uri field)
 * - Verify SSL certificate if verifySSL is true
 * - Check for validation string if specified
 * - Report response time and availability
 */

// This script will not be executed for SIMPLE monitor types
console.log('Simple monitor configuration loaded');
